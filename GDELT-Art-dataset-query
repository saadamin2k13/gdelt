SELECT * 
    FROM `gdelt-bq.gdeltv2.ggg` 
    WHERE DATE(DateTime) = "2021-10-21"
    and Title like ('% art %')
    and LangCode = "eng"
    and DomainCountryCode = "US"
    and CountryCode = "US"
LIMIT 5000
