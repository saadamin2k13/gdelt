SELECT * 
    FROM `gdelt-bq.gdeltv2.ggg` 
    WHERE DATE(DateTime) = "2020-07-15"
    and Title like ('% culture %')
    and LangCode = "eng"
    and DomainCountryCode = "US"
    and CountryCode = "US"
LIMIT 5000
